/**
Combining Arrays
Instructions:

Create two arrays of your choice.
Use the concat method to combine the two arrays into a new array.
Print both the original arrays and the combined array using console.log().
*/

// Low-level languages
let lowLevelLanguages = ["Assembly", "C (system programming)", "Machine Code"];

// High-level languages
let highLevelLanguages = ["Python", "Java", "JavaScript", "C++", "C#", "Ruby", "PHP", "Swift", "Kotlin"];

let concatenate = lowLevelLanguages.concat(highLevelLanguages);
console.log(concatenate);    